# DeskLink releases

- `host/` contains complete DeskLink release ZIPs with the Windows host EXE and viewer ZIP.
- `viewer/` contains the current browser-only viewer ZIP.

Keep the newest release here. Older versions are kept only for rollback or comparison.
